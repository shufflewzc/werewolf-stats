# Match Result Templates

`match-result-upload-template.xlsx` is the current Excel draft for batch match result import.

Regenerate it with:

```powershell
python scripts/generate_match_result_excel_template.py
```

# Reject traffic whose Host/SNI is not the production domain instead of routing
# it through the first configured virtual host.
server {
    listen 80 default_server;
    server_name _;
    return 444;
}

server {
    listen 443 ssl default_server;
    server_name _;
    ssl_reject_handshake on;
}

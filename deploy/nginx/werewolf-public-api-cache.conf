# Include this file from the `http` block (for example via /etc/nginx/conf.d/).
proxy_cache_path /var/cache/nginx/werewolf-public-api
  levels=1:2
  keys_zone=werewolf_public_api:10m
  inactive=10m
  max_size=100m
  use_temp_path=off;
